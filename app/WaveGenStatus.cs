namespace PowerSupplyApp
{
    public enum WaveGenStatus
    {
        Ok,
        FileDoesNotExit,
        InvalidFilePath,
        InvalidFile,
        NotLoaded,
        SetpointFailure
    }
}
